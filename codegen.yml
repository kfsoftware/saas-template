schema: 
- http://localhost:3000/api/graphql
generates:
  ./operations/index.ts:
    documents:
      - ./operations/**/*.graphql
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
